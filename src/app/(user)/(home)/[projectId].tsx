import React from 'react';
import ProjectDetails from '@/src/components/ProjectDetails';

type Props = {};

const ProjectInfoById = (props: Props) => {
    return (<ProjectDetails />

    );
};

export default ProjectInfoById;